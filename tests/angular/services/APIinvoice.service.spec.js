ngDescribe({
    name: 'Test APIinvoiceService',
    modules: 'app',
    inject: 'APIinvoiceService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
