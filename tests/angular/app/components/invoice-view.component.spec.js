ngDescribe({
    name: 'Test invoice-view component',
    modules: 'app',
    element: '<invoice-view></invoice-view>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
